[package]
name = "tetrust"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.17.3"
rand = "0.8"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

[features]
dynamic_linking = ["bevy/dynamic_linking"]